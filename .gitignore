node_modules/
dist/
# Salida del build de la landing (CARRIL 1A). Va aparte de `dist/` a
# propósito: son dos artefactos y dos deploy targets distintos, y compartir
# carpeta volvería esa separación una afirmación en vez de un hecho.
dist-landing/
# Config efímera de la sonda de aislamiento de tipos
# (`scripts/tsProjectIsolation.test.ts`). Se borra en un `finally` y en el
# `afterAll`; esto es la red por si el proceso muere de golpe.
.tsprobe.json
*.local
.DS_Store

# ── Playwright (ORDEN 5) ────────────────────────────────────
# Salida de corridas: trazas, capturas y reporte HTML. Se regeneran, y una
# captura de una corrida fallida puede tener datos de la sesión mock adentro.
test-results/
playwright-report/
blob-report/
.playwright/

# ── Variables de entorno ────────────────────────────────────
# Ignorar TODO archivo .env* (puede traer secretos o config local)…
.env
.env.*
# …salvo la config de dev no-secreta (versionada a propósito: VITE_MOCK=1)
# y los archivos de ejemplo.
!.env.development
!.env.mock
!*.example
