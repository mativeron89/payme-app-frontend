# Mock-first: la app entera navegable sin backend (regla dura #2 del CLAUDE.md).
# Para conectar al backend real local (T7): VITE_MOCK=0 y backend en :3000.
VITE_MOCK=1
VITE_API_URL=http://localhost:3000
